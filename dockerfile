# Используем образ Python Slim как базовый образ
FROM python:slim

# Устанавливаем переменную среды TOKEN со значением, которое будет использоваться внутри контейнера
ENV TOKEN = 'Your Token'

# Копируем все файлы из текущего каталога в рабочую директорию внутри контейнера
COPY . .

# Устанавливаем зависимости, перечисленные в файле requar.txt
RUN pip install -r requar.txt

# Запускаем команду, которая будет выполняться при запуске контейнера
CMD python bot.py
